const Request = require('./Request.js');
const Response = require('./Response.js');

module.exports.default = class Message {
    constructor() {
        messageHash = '';
        messageBody = {};
        
    }
}